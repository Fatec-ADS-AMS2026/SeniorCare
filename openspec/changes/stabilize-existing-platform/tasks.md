## 1. Baseline e infraestrutura de testes

- [x] 1.1 Instalar/restaurar o SDK .NET 8 e registrar os comandos reproduzíveis de build dos três componentes.
- [x] 1.2 Corrigir a incompatibilidade de `ApiResponse` no front-end assistencial e comprovar lint e build limpos.
- [x] 1.3 Criar projetos de testes unitários e de integração do backend e adicioná-los à solution.
- [x] 1.4 Configurar PostgreSQL efêmero para testes de integração, com health check, migração desde banco vazio e descarte ao final.
- [x] 1.5 Configurar runner, biblioteca de componentes, DOM de teste, mock HTTP e coleta de cobertura em cada front-end.
- [x] 1.6 Adicionar testes de caracterização dos CRUDs atuais antes de alterar seus contratos.

## 2. Configuração e diagnóstico de execução

- [x] 2.1 Substituir a URL absoluta da API por caminho de mesma origem nos dois clientes HTTP.
- [x] 2.2 Configurar proxy de desenvolvimento nos dois projetos Vite com destino externo e validação de formato.
- [x] 2.3 Configurar o nginx de cada front-end para encaminhar `/api` à API na rede Docker.
- [x] 2.4 Adicionar modelos de configuração sem segredos para desenvolvimento, teste e produção e documentar todas as variáveis obrigatórias.
- [x] 2.5 Adicionar validação de startup para conexão, chaves, instituição e bootstrap, garantindo mensagens sem valores secretos. Validação de `ConnectionStrings:DefaultConnection` e das três variáveis de bootstrap (`Bootstrap__InstitutionName`/`Bootstrap__AdminEmail`/`Bootstrap__AdminDisplayName`, exigidas todas juntas ou nenhuma) implementada e testada em `Program.GetMissingConfiguration`. Chaves de sessão/JWT ainda não se aplicam — dependem da capability de sessão (§7).
- [x] 2.6 Separar endpoints de vida e prontidão e testar o comportamento com banco disponível e indisponível.
- [x] 2.7 Verificar nos bundles de produção que não há destino operacional em `localhost` nem segredos incorporados.

## 3. Contratos HTTP e tratamento de erros

- [x] 3.1 Definir DTOs de criação, atualização, resposta e listagem paginada sem expor entidades de persistência.
- [x] 3.2 Implementar Problem Details centralizado com códigos estáveis, erros por campo e identificador de correlação.
- [x] 3.3 Remover mensagens de exceção das respostas e adicionar testes que impeçam vazamento de detalhes internos.
- [x] 3.4 Padronizar 400/401/403/404/409/422/500 em todos os controllers existentes. **Parcial**: 400 (validação de forma via ModelState), 404 (recurso ausente), 409 (conflito de concorrência, tarefa 3.7) e 500 (erro genérico) uniformes via `GlobalExceptionHandler`; 422 introduzido para violação de regra de negócio (FK inválida em ProductType). 401/403 dependem de identidade/autorização (capability `platform-authentication`, §4 em diante) — reabrir quando esse trabalho chegar.
- [x] 3.5 Implementar paginação e filtro consistentes nos dez catálogos definidos pela spec. **Parcial**: paginação (`page`/`pageSize`) e filtro (`search`) aplicados aos 9 catálogos existentes via `PagedResult<T>`/`CatalogQuery`. O 10º catálogo (Produto) ainda não existe — é criado na tarefa 9.4 e deve seguir o mesmo padrão quando implementado.
- [x] 3.6 Tornar o ID da rota canônico, rejeitar divergência com o corpo e remover o `PATCH` que executa substituição total. `*UpdateRequest` nunca teve campo `Id` (só a rota define o alvo); `JsonUnmappedMemberHandling.Disallow` rejeita com 400 qualquer campo desconhecido no corpo (inclusive um "id" divergente). PATCH de substituição total removido de ProductGroup/ProductType/HealthInsurancePlan (as únicas 3 que tinham).
- [x] 3.7 Adicionar concorrência otimista aos catálogos e testes para edição com versão desatualizada. Token de concorrência via `xmin` do Postgres (shadow property `Version`, `IsRowVersion()`), exposto como `RowVersion` nos DTOs de resposta e exigido nos `*UpdateRequest`. `DbUpdateConcurrencyException` mapeada para 409 pelo `GlobalExceptionHandler`. Verificado com teste de integração real (Postgres via Testcontainers): segunda edição com `RowVersion` desatualizado retorna 409.
- [x] 3.8 Publicar e validar o contrato OpenAPI resultante contra os clientes dos dois front-ends. `/swagger/v1/swagger.json` disponível em todo ambiente (antes só em Development). Teste de integração automatizado lê os `*Service.ts` reais dos dois front-ends e confirma que cada rota efetivamente chamada existe no contrato com os verbos certos. **Achado**: `stock-web` já chama `api/v1/Product` (código pronto), mas a entidade só existe na tarefa 9.4 — lacuna conhecida, documentada explicitamente no teste (`StockWeb_Product_IsAKnownGapNotYetInContract`), não corrigida aqui (fora de escopo de §3).

## 4. Instituição, identidade e política de senha

- [x] 4.1 Modelar instituição e estender a identidade ASP.NET com `InstitutionId`, `IdentityOrigin` e estados `PROVISIONED`, `ACTIVE`, `INACTIVE`, `BLOCKED` e `EXPIRED`. `Institution` (chave própria) + `ApplicationUser : IdentityUser<Guid>` com `InstitutionId`/`IdentityOrigin`/`AccountState`/`MfaEnabled`. `AppDbContext` passou a herdar `IdentityUserContext<ApplicationUser, Guid>` (não `IdentityDbContext` completo) — sem `Roles`/`UserRoles`, para não colidir com o RBAC próprio da §5.
- [x] 4.2 Criar migração aditiva para instituição, usuários e credenciais locais, com índices e restrições de isolamento institucional. Migração `AddInstitutionAndIdentity`: `institution`, `AspNetUsers`/`AspNetUserClaims`/`AspNetUserLogins`/`AspNetUserTokens`, `accounttoken` — só `CreateTable`/`CreateIndex`, nenhuma tabela existente alterada. Índice único `(InstitutionId, NormalizedEmail)` e `(InstitutionId, NormalizedUserName)`; o índice global de `NormalizedUserName` do Identity padrão foi explicitamente tornado não-único para não vazar unicidade entre instituições.
- [x] 4.3 Implementar origem `LOCAL` e pontos de extensão desabilitados para `LDAP` e `OIDC`, rejeitando ativação sem provedor real. `InstitutionIdentityOriginService.EnsureOriginAvailable` lança `BusinessRuleException` (422) para `LDAP`/`OIDC`; pronto para ser exposto por API administrativa na §6.
- [x] 4.4 Configurar derivação adaptativa de senha com salt individual, atualização transparente de parâmetros e ausência de segredos em respostas e logs. Usa o `PasswordHasher<ApplicationUser>` padrão do ASP.NET Identity (PBKDF2-HMACSHA256 versionado, rehash automático via `UserManager`); nenhum DTO de resposta expõe hash/segredo.
- [x] 4.5 Implementar piso de senha: 15 caracteres sem MFA ou 8 com MFA obrigatório, aceitação de pelo menos 64 caracteres, espaços e Unicode. `PasswordPolicyService`/`InstitutionalPasswordPolicyValidator` (substitui as regras de composição padrão do Identity, desligadas em `Startup.cs`); limite técnico máximo de 256 caracteres (proteção contra abuso de hashing, não conflita com o piso de 64).
- [x] 4.6 Integrar bloqueio de senhas comuns ou comprometidas sem regra arbitrária de composição nem expiração periódica automática. `CommonPasswordBlocklist`: lista local embutida (`Data/CommonPasswords.txt`, *embedded resource*), sem dependência de serviço externo — mantém CI/testes offline. Nenhuma regra de composição por classe de caractere nem expiração por tempo foi adicionada.
- [x] 4.7 Implementar configuração institucional que somente fortaleça os pisos de senha e adicionar validações de limites. `Institution.MinPasswordLengthWithoutMfaOverride`/`MinPasswordLengthWithMfaOverride` + `PasswordPolicyService.ValidateInstitutionOverride`, que rejeita qualquer valor abaixo do piso global.
- [x] 4.8 Implementar bootstrap idempotente da instituição e do administrador `PROVISIONED`, sem senha fixa ou redefinição silenciosa. `BootstrapService`, chamado em `Program.cs` após a migração: no-op se já existir instituição; caso contrário, lê `Bootstrap__InstitutionName`/`Bootstrap__AdminEmail`/`Bootstrap__AdminDisplayName` e cria instituição + admin `PROVISIONED` sem senha. Link de ativação impresso uma única vez no console do primeiro boot.
- [x] 4.9 Implementar ativação por token aleatório, curto, de uso único e armazenado por hash, com definição da senha pela própria pessoa. `AccountTokenService` (não usa os token providers padrão do Identity, que não são "armazenados por hash") + `POST /api/v1/Auth/activate`.
- [x] 4.10 Implementar recuperação com resposta antienumeração, token protegido e revogação das sessões após redefinição. `POST /api/v1/Auth/recover` sempre 200 com corpo idêntico; `POST /api/v1/Auth/reset-password` consome o token e troca a senha (o `SecurityStamp` é rotacionado automaticamente pelo `UserManager`, invalidando qualquer coisa emitida antes — sessão real só existe a partir da §7).
- [x] 4.11 Implementar mudança autenticada de senha com senha atual ou reautenticação recente e revogação das demais sessões. `POST /api/v1/Auth/change-password` — endpoint autocontido (recebe e-mail + senha atual + nova senha no corpo) porque sessão/login só existem a partir da §7; usa `UserManager.ChangePasswordAsync`, que já rotaciona o `SecurityStamp`.
- [x] 4.12 Adicionar testes de isolamento institucional, ciclo de vida, política de senha, bootstrap, ativação, recuperação e ausência de segredos. Unit: `PasswordPolicyServiceTests`, `CommonPasswordBlocklistTests`, `InstitutionIdentityOriginServiceTests`. Integração (Postgres real via Testcontainers): `BootstrapServiceTests` (idempotência), `AuthControllerTests` (ativação com token válido/reutilizado/expirado, senha abaixo do piso, recuperação antienumeração, troca de senha com credencial errada/correta).

## 5. Modelo e decisão de acesso

- [x] 5.1 Modelar `Role`, `Permission(resource, action, feature)`, `PermissionGroup` e as associações de composição, todos delimitados pela instituição quando aplicável. `Permission`/`PermissionGroup` são globais (vocabulário fixo do sistema); `Role` é por instituição, composto de `PermissionGroup` via `RolePermissionGroup`, atribuído via `UserRole`.
- [x] 5.2 Modelar responsabilidade organizacional e atribuições por instituição, unidade ou setor, com início e término de validade. `OrganizationalRole` (por instituição, composto de `PermissionGroup`) + `OrganizationalRoleAssignment` (`ScopeType` INSTITUTION/UNIT/SECTOR + `ScopeKey` livre — sem catálogo de Unidade/Setor ainda —, `ValidFrom`/`ValidTo`).
- [x] 5.3 Modelar exceções individuais `ALLOW`/`DENY` com permissão, escopo, justificativa, autoria e validade. `UserPermissionOverride`; `IAccessDecisionService.ValidateOverrideJustification` rejeita exceção permanente (`ValidTo == null`) sem justificativa não-trivial.
- [x] 5.4 Modelar políticas condicionais de concessão e negação com versão, estado e condições estritamente validadas. `AccessPolicy` (colunas tipadas, não expressão livre) — editar cria nova linha com `Version+1` e aposenta a anterior (`State=RETIRED`), histórico real.
- [x] 5.5 Criar migração aditiva das entidades de autorização e seeds somente de permissões sistêmicas, sem usuários ou senhas padrão. Migração `AddAccessControlFoundation`: 11 `CreateTable` + 1 `AddColumn` (`IsSystemAdmin`), seed de 27 `Permission` (9 catálogos × read/write/delete) — nenhum `Role`/`PermissionGroup`/atribuição/usuário/senha padrão.
- [x] 5.6 Implementar `AccessDecisionService` central por instituição, recurso, ação, funcionalidade e escopo-alvo. `AccessDecisionService.EvaluateAsync` — carrega o usuário fresco do banco, nunca confia em claim de instituição do cliente.
- [x] 5.7 Implementar precedência: contexto/estado inválido, `SYSTEM_ADMIN`, `DENY` individual, política `DENY`, `ALLOW` individual, política `ALLOW`, RBAC e `DENY` padrão. Implementado exatamente nessa ordem; coberto por `AccessDecisionServiceTests` (matriz de precedência).
- [x] 5.8 Restringir `SYSTEM_ADMIN` a operações sistêmicas, impedir sua atribuição a usuários operacionais e destacar todo uso na auditoria. `ApplicationUser.IsSystemAdmin` — flag fora do sistema `Role`/`PermissionGroup`, nenhuma API concede; bypass só vale para `Permission.IsSystemOperation=true` (nenhuma das 27 seeds é marcada assim). Auditoria de uso é §8.
- [x] 5.9 Garantir que profissão/cargo não conceda acesso e que responsabilidade organizacional conceda somente capacidades configuradas e vigentes. `Position` (catálogo existente) nunca é consultado pelo `AccessDecisionService`; `OrganizationalRoleAssignment` só concede as `PermissionGroup` explicitamente ligadas ao `OrganizationalRole`, dentro da validade e do escopo.
- [x] 5.10 Proteger todos os endpoints existentes pelo serviço de decisão, com HTTP 401 para anonimato e HTTP 403 para decisão negativa. `[Authorize]` global (`AuthorizeFilter`) + `[RequirePermission(resource, action)]` nas 45 ações dos 9 catálogos; `AuthController` com `[AllowAnonymous]` de classe. **Consequência aceita**: sem API administrativa (§6) nem login real (§7) ainda, nenhuma requisição HTTP real consegue mais acessar os catálogos até essas seções chegarem — intencional (negação padrão antes de existir caminho de concessão).
- [x] 5.11 Implementar invalidação/versionamento do contexto efetivo quando papel, grupo, vínculo, exceção ou política mudar. `AccessDecisionService` não faz cache — cada chamada lê o estado atual do banco, então qualquer mudança já vale na próxima avaliação. Não há "contexto efetivo do cliente" pra invalidar ainda (nasce com o endpoint de identidade atual, §6.8, e a sessão, §7).
- [x] 5.12 Criar testes matriciais de precedência, negação padrão, escopo, validade, conflito e manipulação do cliente. `AccessDecisionServiceTests` (precedência, escopo SECTOR, atribuição expirada, SYSTEM_ADMIN restrito, conta inativa) + `EndpointAuthorizationTests` (401/403/200 HTTP reais via `TestAuthHandler`).
- [x] 5.13 Documentar que acesso administrativo não concede autorização clínica futura. Nota em `design.md` (decisão 7).

## 6. Configuração administrativa de acesso

- [x] 6.1 Criar APIs protegidas para listar, criar, ativar, inativar, bloquear e expirar contas sem expor credenciais. `AdminUserController` (`AdminUser`: read/write) + `AdminUserService` (mesmo princípio do bootstrap, §4: cria PROVISIONED e emite token de ativação, nunca conhece senha). Sem exclusão física — só transição de estado.
- [x] 6.2 Criar APIs protegidas para papéis, permissões, grupos e suas associações, com validação e histórico. `AdminRoleController`, `AdminPermissionGroupController` (CRUD + anexar/remover `Permission`/`PermissionGroup`), `AdminPermissionController` (só leitura — vocabulário fixo do sistema, §5). Histórico de antes/depois fica para a §8 (auditoria); aqui as mutações são diretas e validadas (referência inexistente → 422).
- [x] 6.3 Criar APIs protegidas para responsabilidades organizacionais, atribuições e validade por escopo. `AdminOrganizationalRoleController` + `AdminOrganizationalRoleAssignmentController` (criar e encerrar antecipadamente via `ValidTo`), com verificação de que usuário e responsabilidade pertencem à instituição de quem está chamando.
- [x] 6.4 Criar APIs protegidas para exceções individuais e políticas condicionais, exigindo justificativa nos casos definidos. `AdminUserPermissionOverrideController` (reusa `IAccessDecisionService.ValidateOverrideJustification` da §5 para exigir justificativa não-trivial em exceção permanente) + `AdminAccessPolicyController` (nova versão via `PolicyKey`+`Version+1`, ativar aposenta a versão anteriormente ativa — histórico real, não campo mutável).
- [x] 6.5 Criar API de política institucional para bloqueio, duração de sessão, MFA e fortalecimento de senha com limites seguros. `AdminInstitutionSecurityController` + `InstitutionSecurityPolicyService` (faixas mínimas/máximas seguras, ex. bloqueio 1–1440 min, tentativas 3–20, acesso 1–60 min, renovação 1–90 dias); campos novos em `Institution`. Consumidos de fato pela §7.
- [x] 6.6 Criar API de sessões ativas com revogação individual ou de todas as sessões de uma conta. Entidade `UserSession` nova (mínima — a §7 estende com colunas aditivas quando implementar login de verdade) + `AdminUserSessionController` (listar, revogar uma ou todas).
- [x] 6.7 Impedir inativação do último administrador institucional ativo e exigir reautenticação para alterações críticas. "Administrador institucional" = conta `ACTIVE` para quem `AccessDecisionService` concede a permissão marcadora `AccessAdministration`/`manage` (incluída no grupo "Acesso Total" do bootstrap). `AdminInvariantService.EnsureNotLastActiveAdministratorAsync` bloqueia (422) a última. Reautenticação via `CurrentPassword` no corpo (mesmo padrão do `AuthController`, §4) nas ações críticas: transição de estado de conta, parâmetros de segurança institucionais, revogação de sessão.
- [x] 6.8 Criar endpoint da identidade atual com instituição, módulos, papéis, responsabilidades e permissões efetivas, sem detalhes internos sensíveis. `GET /api/v1/Auth/me` — autenticado (não anônimo, ao contrário dos outros 4 endpoints de `AuthController`); permissões efetivas calculadas avaliando `AccessDecisionService` contra cada `Permission` cadastrada.
- [x] 6.9 Criar endpoint administrativo de explicação de decisão com acesso restrito e dados suficientes para suporte e auditoria. `POST /api/v1/AdminAccessDecision/explain` — devolve `AccessDecision` (`Allowed`+`DeterminingLayer`), nunca a linha exata (override/política) que decidiu.
- [x] 6.10 Adicionar testes de autorização administrativa, invariantes, concorrência, histórico e invalidação de contexto. Um arquivo de teste de integração por controller (403 sem permissão, sucesso com ela, validação, isolamento institucional), casos dedicados de invariante do último administrador, reautenticação, concorrência otimista (`Role`/`PermissionGroup`/`OrganizationalRole` ganharam `Version`/`xmin`), `/me` refletindo mudança imediatamente anterior sem cache, e `BootstrapServiceTests` confirmando o `Role` "Administrador" com todas as permissões.

**Achado registrado, não uma tarefa numerada**: `BootstrapService` (§4) precisou ganhar um passo adicional — criar `PermissionGroup`/`Role` "Administrador" com todas as `Permission` existentes e atribuí-lo ao admin — porque sem isso a §6 entregaria endpoints reais e permanentemente inacessíveis (o mesmo impasse que a §5 deixou em aberto). Mudança aditiva ao fluxo idempotente já em produção, sem alterar seu comportamento externo (`BootstrapResult` inalterado).

## 7. Sessão, MFA e proteção contra abuso

- [x] 7.1 Implementar login com resposta genérica, acesso de curta duração e contexto institucional explícito. `AuthController.Login`: busca por e-mail, checa bloqueio, checa `AccountState==ACTIVE`, `CheckPasswordAsync` — qualquer falha (e-mail inexistente, senha errada, conta não ativa) devolve o mesmo 401 genérico, nunca revela o motivo (§4). "Acesso de curta duração" é a janela de rotação da própria sessão (`AccessTokenDurationMinutes`, default 15 min, configurável por instituição — §6), não um token de acesso separado (decisão: cookie único, sem bearer — ver design.md). Contexto institucional vem embutido na claim `InstitutionId` (§5) e no `CurrentIdentityDTO` retornado.
- [x] 7.2 Persistir famílias de renovação por hash e implementar cookie `HttpOnly`, `Secure` e `SameSite` com proteção CSRF adequada. `UserSession.CurrentKeyHash`/`PreviousKeyHash` (SHA-256, a chave nunca é gravada em claro) — a "família de renovação" é a própria cadeia de rotação da sessão, não um conjunto de refresh tokens à parte (consequência de usar cookie único). `HttpOnly`+`SameSite=Strict`+`Secure=Always` já estavam registrados na §5, reaproveitados sem alteração. CSRF: `SameSite=Strict` + allow-list de CORS + corpo exclusivamente JSON cobrem o requisito sem precisar de um token anti-CSRF de dupla submissão separado (decisão registrada no design).
- [x] 7.3 Implementar rotação de renovação, detecção de reutilização, revogação individual, revogação por conta e logout. `SessionService.ValidateAndRotateAsync`, chamado a cada requisição autenticada via `Events.OnValidatePrincipal` (`Startup.cs`) — não `ITicketStore` nem `SlidingExpiration` automático, para controle explícito sobre reuso. Chave apresentada batendo com `PreviousKeyHash` (uma chave já rotacionada sendo reapresentada) revoga a sessão inteira, não só a chave. Revogação individual/por conta já existia desde a §6 (`AdminUserSessionController`), agora delegando para `SessionService.RevokeAsync`/`RevokeAllForUserAsync`. `AuthController.Logout` revoga a sessão atual e encerra o cookie via `SignOutAsync`.
- [x] 7.4 Compartilhar a sessão de mesma origem entre os módulos assistencial e estoque sem persistir credenciais em `localStorage` ou `sessionStorage`. Cookie `HttpOnly` por definição nunca é lido por JS — não há alternativa de armazenamento a considerar. Nova variável opcional `SessionCookieDomain` (`CONFIGURATION.md`) escopa o cookie pro domínio pai quando os dois front-ends ficam em subdomínios distintos em produção; sem valor, cookie host-only (correto em dev, onde os dois front-ends já compartilham host).
- [x] 7.5 Implementar MFA TOTP e códigos de recuperação de uso único com armazenamento protegido. `.AddDefaultTokenProviders()` habilita `AuthenticatorTokenProvider`/`RecoveryCodeTokenProvider` nativos do Identity — segredo TOTP e códigos de recuperação ficam em `AspNetUserTokens`, sem reimplementar RFC 6238/armazenamento. `TwoFactorEnabled` nativo do `IdentityUser<Guid>` passa a ser a única fonte da verdade (o campo `ApplicationUser.MfaEnabled`, da §4, foi removido). `mfa/enroll`/`mfa/confirm` aceitam sessão autenticada já existente (cadastro voluntário) **ou** `ChallengeToken` de propósito `MFA_ENROLLMENT` (cadastro obrigatório pré-sessão, §7.7). Login concluído via código de recuperação devolve `LoginResponse.RemainingRecoveryCodes` (via `UserManager.CountRecoveryCodesAsync`) — alerta a quantidade restante, exigido pelo cenário "Código de recuperação" da spec.
- [x] 7.6 Exigir MFA para administradores e contas de configuração de acesso e permitir obrigatoriedade institucional para os demais. `IMfaPolicyService.IsMfaRequiredAsync`: `IsSystemAdmin` OU permissão marcadora `AccessAdministration`/`manage` (§6) OU `Institution.MfaRequiredForAllUsers` (coluna da §6, sem consumidor até agora).
- [x] 7.7 Restringir contas privilegiadas sem MFA ao fluxo de cadastro e recuperação do segundo fator. Login com MFA obrigatório mas `TwoFactorEnabled==false` nunca chega a criar sessão — devolve `{status: "mfa_enrollment_required", challengeToken}`; só `mfa/enroll`+`mfa/confirm` com esse token completam o login (nenhum outro endpoint aceita esse `ChallengeToken`). Um código TOTP errado não queima o desafio: `IAccountTokenService` ganhou `ValidateAsync` (só confere validade, não consome) — só a tentativa que efetivamente completa o login chama `ConsumeAsync`.
- [x] 7.8 Implementar limitação por conta e origem, atraso progressivo ou bloqueio temporário, com valores padrão e configuração segura. Por conta: reusa `AccessFailedCount`/`LockoutEnd` nativos do Identity — `RegisterFailedAttemptAsync` orquestra manualmente com limites configuráveis por instituição (`MaxFailedAttempts`/`LockoutDurationMinutes`, default seguro 5/15 min, §6); `options.Lockout.MaxFailedAccessAttempts=int.MaxValue` desliga o gatilho automático do Identity para não haver dois mecanismos decidindo a mesma coisa. Por origem: novo `IOriginRateLimiter` (singleton em memória, janela deslizante por IP, default 20 falhas/15 min, com limpeza best-effort das entradas expiradas — não cresce sem limite) — limite conhecido documentado (não sobrevive a reinício nem escala a múltiplas réplicas). "Atraso progressivo" não foi implementado: bloqueio temporário já cobre o requisito sem complexidade redundante (decisão registrada no design). `Startup.Configure` chama `UseForwardedHeaders` (X-Forwarded-For/Proto) como primeiro middleware — em produção a API só é alcançada via o nginx dos front-ends, e sem isso `IOriginRateLimiter`/`UserSession.IpAddress` veriam sempre o IP do proxy, tornando o limitador por origem efetivamente global. A resposta de conta bloqueada é o mesmo 401 genérico das demais falhas de login (não um 429 distinto) — um status diferenciado seria um oráculo de enumeração de contas bloqueadas, contrariando a exigência de "resposta uniforme... sem permitir enumeração de usuários" do próprio requisito.
- [x] 7.9 Adicionar testes HTTP de login, MFA, expiração, rotação, reutilização, logout, 401, limitação e revogação após mudança de conta ou senha. 15 testes de integração em 5 arquivos: `AuthControllerLoginTests`, `AuthControllerAccountLockoutTests`, `AuthControllerOriginRateLimitTests` (cada limitador de força bruta isolado na própria classe — `IClassFixture` cria um singleton de `IOriginRateLimiter` por classe, não por teste), `AuthControllerMfaTests`, `SessionRotationTests` (rotação, reuso revoga a sessão inteira, logout revoga, troca de senha revoga todas as sessões). Três achados de infraestrutura de teste corrigidos ao longo do caminho: (a) `AuthenticatorTokenProvider.GenerateAsync` do próprio ASP.NET Core Identity sempre retorna `null` — só `ValidateAsync` é implementado, porque na vida real o código vem de um app autenticador externo, nunca do servidor; os testes passaram a computar o código TOTP com uma implementação própria de RFC 6238 a partir do segredo (verificada contra `ValidateAsync` antes de adotada); (b) usuários de teste inseridos direto via `DbContext` (fora de `UserManager.CreateAsync`) precisam de `SecurityStamp` e `LockoutEnabled=true` explícitos — sem isso, `UpdateAsync` falha silenciosamente (chaves TOTP nunca persistem) e `IsLockedOutAsync` nunca bloqueia; (c) com `Cookie.SecurePolicy=Always` (§5), o `CookieContainer` do `HttpClient` de teste só reenvia o cookie de sessão em requisições subsequentes se o cliente usar uma base address `https://` — acrescentado `PostgresWebApplicationFactory.CreateAuthenticatedFlowClient()` para isso.

## 8. Auditoria de identidade e acesso

- [x] 8.1 Criar modelo e migração append-only para eventos de autenticação, sessão, configuração, catálogo e decisão de acesso. `AuditEvent` (`Objects/Models/AuditEvent.cs`) + `AuditEventCategory` com exatamente as 5 categorias do requisito. Migração `AddAuditEvent` — aditiva (`CreateTable`), sem alterar nenhuma tabela existente.
- [x] 8.2 Capturar ator, instituição, recurso, ação, funcionalidade, escopo-alvo, instante UTC, correlação, resultado e camada determinante. `IAuditService.RecordAsync` (`Services/Entities/AuditService.cs`) — todos os campos do requisito são parâmetros explícitos; `Resource`/`Action`/`Feature` reaproveitam o mesmo vocabulário de `Permission`/`RequirePermissionAttribute` (§5), em vez de um enum por tipo de evento. `CorrelationId` vem de `HttpContext.TraceIdentifier` — mesmo identificador que `ProblemDetails.Extensions["correlationId"]` já expõe em respostas de erro (`Startup.cs`), a auditoria passa a ser o segundo consumidor. `DeterminingLayer` só é preenchido em `ACCESS_DECISION` e replica literalmente `AccessDecision.DeterminingLayer` (`AccessDecisionService`).
- [x] 8.3 Registrar login, logout, bloqueio, MFA, ativação, recuperação, mudança de senha, estados de conta e revogação de sessão. Todos os pontos de `AuthController` (login em cada ramo — sucesso, MFA obrigatório, senha errada, conta bloqueada/inativa —, `login/mfa`, `mfa/confirm`, `logout`, `activate`, `recover`, `reset-password`, `change-password`) mais `AdminUserController.ChangeState` (estado de conta) e `AdminUserSessionController.Revoke`/`RevokeAll` (revogação de sessão) chamam `IAuditService.RecordAsync` explicitamente após cada ação ter sucesso/falhado — sem interceptor de captura automática (nenhum precedente no código; toda escrita já era uma chamada explícita a `SaveChangesAsync`, então a auditoria segue a mesma convenção). Falhas de login (e-mail inexistente, senha errada, conta bloqueada) todas viram o mesmo `Outcome=FAILURE` — a auditoria interna não reproduz a distinção que a resposta ao cliente esconde de propósito (§7.8).
- [x] 8.4 Registrar versões anterior e posterior permitidas de papel, grupo, vínculo, exceção, política e parâmetro de segurança. `AdminRoleController`, `AdminPermissionGroupController`, `AdminOrganizationalRoleController`/`AdminOrganizationalRoleAssignmentController`, `AdminUserPermissionOverrideController`, `AdminAccessPolicyController` e `AdminInstitutionSecurityController` — toda criação/edição/exclusão/vínculo chama `IAuditService.RecordAsync` com `Category=CONFIGURATION` e um snapshot mínimo antes/depois (ex.: `new { Name = previousName }`/`new { role.Name }`), nunca a entidade EF inteira. Os 9 catálogos simples (§3b) ganham `Category=CATALOG` num único ponto — `GenericRepository<T>` (único lugar onde os 9 já escreviam, §8.1) — em vez de repetir a chamada em cada controller; `Update` passou a ler o valor anterior via `AsNoTracking()` antes de aplicar a mudança (antes não lia o estado anterior). `GenericRepository` serializa a entidade inteira só pros 6 catálogos que são dado de referência puro — `Carrier`/`Manufacturer`/`Supplier` têm CPF/CNPJ, e-mail, telefone e endereço de terceiro (fornecedor/transportadora) e usam um snapshot reduzido (`Id`+`CorporateName`+`TradeName`), achado da revisão do PR: gravar esse dado pessoal por inteiro numa tabela agora imutável (§8.6) seria retenção indevida sem forma de corrigir/expurgar depois. Coberto por `AuditEventTests.CreateCarrier_RedactsPiiFieldsInAuditEvent`.
- [x] 8.5 Registrar decisões protegidas e uso de `SYSTEM_ADMIN` sem payload integral, senha, token, segredo ou código MFA. `RequirePermissionAttribute` (não `AccessDecisionService.EvaluateAsync` diretamente — esse também é chamado por `BuildCurrentIdentityAsync` pra listar toda permissão efetiva em todo `/me`, o que inundaria a auditoria) é o único ponto que registra `ACCESS_DECISION`, cobrindo "decisão protegida" e "uso de `SYSTEM_ADMIN`" (um dos valores possíveis de `DeterminingLayer`) com um único hook — na prática grava em toda requisição a um endpoint `[RequirePermission]`, inclusive leituras (95 usos em 21 controllers), não só escritas/negações; amplia o risco de crescimento sem política de retenção que o `design.md` já registrava em aberto, não introduz um risco novo (achado da revisão do PR, corrigido aqui: a nota original subestimava o volume). "Sem payload integral/segredo": `BeforeValue`/`AfterValue` são sempre um snapshot mínimo montado por quem chama, nunca `JsonSerializer.Serialize(entidadeInteira)` — garantido por convenção em todo call site, não por um mecanismo automático de redação.
- [x] 8.6 Impedir atualização e exclusão da auditoria pela API e pela camada normal de repositórios. Nenhum endpoint PUT/DELETE para `AuditEvent`. Duas camadas de reforço (decisão confirmada com o usuário — ir além do texto literal do requisito): `AuditImmutabilityInterceptor` (`Data/Interceptors/`, primeiro `SaveChangesInterceptor` do projeto) lança se `ChangeTracker` tiver um `AuditEvent` `Modified`/`Deleted`; e a própria migração cria uma função + trigger Postgres `BEFORE UPDATE OR DELETE ON audit_event` que rejeita a operação no banco (primeira migração do projeto com `migrationBuilder.Sql(...)` — todas as anteriores só usam a API fluente do EF), sobrevivendo a um acesso direto ao banco ou a um bug futuro que bypasse o EF.
- [x] 8.7 Adicionar testes de atribuição, correlação, imutabilidade, camada determinante e ausência de segredos. `AuditEventTests.cs` (7 testes): atribuição (login grava `ActorUserId`/`InstitutionId` certos), correlação (`AdminUserController.ChangeState` pra `INACTIVE` produz um evento `AUTHENTICATION` + um `SESSION` com o mesmo `CorrelationId`), imutabilidade em duas camadas (update/delete direto via `DbContext` lança pelo interceptor; `ExecuteSqlInterpolatedAsync` bruto, contornando o EF de propósito, lança `PostgresException` pela trigger), camada determinante (`DEFAULT_DENY` numa ação negada; `SYSTEM_ADMIN` numa permitida — achado incidental: nenhuma `Permission` do seed atual tem `IsSystemOperation=true`, o bypass está modelado desde a §5 mas nenhum endpoint ainda o usa, então o teste ativa a flag numa permissão só dentro do próprio container de teste pra exercitar o caminho de verdade), ausência de segredos (após troca de senha, nenhuma linha de `AuditEvent` contém a senha bruta usada no teste), e redação de PII de terceiro (`CreateCarrier_RedactsPiiFieldsInAuditEvent` — CPF/CNPJ e e-mail de `Carrier` não vão para `AfterValue`, achado da revisão do PR).

## 9. Catálogos auxiliares e produto

- [x] 9.1 Adicionar estado ativo e token de concorrência aos nove catálogos existentes com migração compatível. O token de concorrência já existia nos 9 (`Property<uint>("Version").IsRowVersion()`, shadow property mapeada pro `xmin` nativo do Postgres, desde antes desta seção) — só faltava o estado ativo. `IsActive` (bool, `HasDefaultValue(true)`) adicionado ao modelo + builder dos 9 (`Position`, `Religion`, `ProductGroup`, `ProductType`, `UnitOfMeasure`, `HealthInsurancePlan`, `Carrier`, `Manufacturer`, `Supplier`), com todas as linhas de seed (`HasData`) atualizadas com `IsActive = true` explícito (a migração gerada não confia no default do banco pra `InsertData`). Migração `AddCatalogActiveState` — aditiva, com backfill (`is_active=true` nas linhas existentes). Cada catálogo também ganhou um índice único no seu identificador natural (`Name` em Position/Religion/ProductGroup, `(ProductGroupId, Name)` composto em ProductType, `Abbreviation` em UnitOfMeasure/HealthInsurancePlan, `CpfCnpj` em Carrier/Manufacturer/Supplier) — nenhum dos 9 tinha unicidade garantida em banco antes.
- [x] 9.2 Substituir exclusão física por inativação e proteger referências contra cascata indevida. `GenericRepository<T>.Remove` não exclui mais — `ToggleActiveAsync` marca `IsActive=false` na mesma instância já rastreada pelo `ChangeTracker` (ver achado abaixo); `Get(bool includeInactive=false)` passa a filtrar ativos por padrão nas listagens, `GetById` continua devolvendo independente do estado (senão não dá pra reativar). Adição além do texto literal da tarefa: `Activate(T entity)` simétrico ao `Remove`, seguindo o padrão já usado em `AccessPolicy` (`Activate`/`Retire`, §6) — sem ele uma inativação por engano vira estado permanente. **Achado de proteção contra cascata**: a única FK real entre catálogos (`ProductType.ProductGroupId`) não tinha `.OnDelete(...)` explícito — caía no default do EF Core, que é `Cascade` — risco real de apagar tipos de produto ao inativar um grupo; corrigido para `DeleteBehavior.Restrict` em `ProductTypeBuilder`. As novas FKs de `Product` (`ProductTypeId`, `UnitOfMeasureId`) já nascem `Restrict`.
- [x] 9.3 Implementar validações de campos, unicidade e referências ativas por catálogo. Unicidade: os índices únicos da 9.1 mapeados no `GlobalExceptionHandler` — **achado corrigido**: violação de índice único (`DbUpdateException{InnerException: PostgresException{SqlState=UniqueViolation}}`) não tinha nenhum tratamento e caía no fallback genérico, expondo um 500 sem explicação pra um duplicado; agora mapeada explicitamente pra 409 com mensagem de "já existe um registro com esse valor" (coberto por `ProductGroupControllerTests.Post_WithDuplicateName_ReturnsConflict`). Referências ativas: `ProductTypeController.EnsureProductGroupExists` passou a exigir também `ProductGroup.IsActive` (não bastava mais existir) — 422 via `BusinessRuleException` se o grupo estiver inativo. Proteção inversa (não inativar algo que tem dependente ativo): `ProductGroupService`/`ProductTypeService`/`UnitOfMeasureService` sobrescrevem `Remove` (agora `virtual` em `GenericService<T>`), cada um injetando `AppDbContext` pra checar dependentes ativos antes de delegar pro `base.Remove` — mesmo padrão pontual já usado em `AdminRoleController.EnsurePermissionGroupExistsAsync`, em vez de um mecanismo genérico de reference-guard.
- [x] 9.4 Criar entidade, configuração, DTOs, repositório, serviço e controller de produto. `Objects/Models/Product.cs` espelha campo por campo o que `ProductForm.tsx`/`Product.ts` do front-end de estoque já usavam havia tempo sem endpoint real por trás (`Description`, `GenericName`, `ProductTypeId`/`UnitOfMeasureId`, `MinimumStock`/`CurrentStock`/`UnitPrice`/`AverageCost?`/`LastPurchasePrice?`/`StockValue?`, `HighCost`/`ExpirationControlled` bool, `IsActive`) — sem tabela de movimento/lote (decisão 5 do `design.md`, explicitamente fora de escopo aqui). `ProductBuilder` (`numeric(14,3)` pra quantidades, `numeric(14,2)` pra valores monetários, ambas FKs `Restrict`), `ProductDTO`/`ProductCreateRequest`/`ProductUpdateRequest` (este com `RowVersion`), `IProductRepository`+`ProductRepository`, `IProductService`+`ProductService`, `ProductController` — todos espelhando a forma exata dos outros 9 catálogos (`CarrierController` como referência mais próxima). **Achado corrigido**: nenhuma linha em `PermissionBuilder.CatalogResources` existia pra "Product" — os 8 testes de `ProductControllerTests` inicialmente falhavam com 403 mesmo pra um usuário "acesso total" seedado, porque "acesso total" só concede permissões que já existem; migração `AddProductPermissions` adiciona as 3 permissões (`read`/`write`/`delete`) que faltavam.
- [x] 9.5 Criar migração de produto com relações a tipo e unidade e pré-validação dos dados existentes. Migração `AddProduct` — cria a tabela do zero (confirmado: nenhum `Product` existia em produção), ambas FKs já `Restrict` desde a criação. "Pré-validação dos dados existentes" fica trivialmente satisfeita por não haver dado legado pra migrar — o que importava de fato era a FK nascer com o `OnDelete` correto, não uma migração de dados. `EnsureReferencesActiveAsync` (`ProductController`) valida em `Post`/`Put` que `ProductTypeId`/`UnitOfMeasureId` referenciam registros existentes **e ativos**, mesma convenção da 9.3.
- [x] 9.6 Implementar pesquisa paginada de produto por descrição e nome genérico. `ProductController.Get` filtra por `CatalogQuery.Search` em `Description`/`GenericName` via `.Contains(...)`, mesmo padrão de paginação/filtro já usado nos 9 catálogos (§3b). `CatalogQuery` ganhou `IncludeInactive` (bool, default `false`) — reaproveitado pelos 9 catálogos existentes também, não só Produto. Coberto por `ProductControllerTests.Search_ByDescriptionOrGenericName_FiltersResults`.
- [x] 9.7 Adaptar o formulário de produto para validação, concorrência, inativação e erros Problem Details. **Achado maior, confirmado com o usuário antes de corrigir (fora do escopo nominal de Produto, mas bloqueando-o)**: o backend já devolve Problem Details real (RFC7807) desde a §3a, mas `handleServiceError`/`serviceUtils.ts` — usado por `generateGenericMethods`, compartilhado por TODAS as telas de catálogo (Carrier, Position, Religion, HealthInsurancePlan, ProductGroup, ProductType, Supplier, UnitOfMeasure, e agora Product) — ainda esperava o formato antigo `{success,message,errors}`; erros já vinham mal-parseados em produção pra TODAS essas telas, não só Produto. Corrigido no utilitário compartilhado (decisão do usuário: consertar uma vez para todos, não duplicar parsing correto só pra Produto): `handleServiceError` agora lê `ProblemDetails` real (`status`/`title`/`detail`/`errors?: Record<string,string[]>`), com um ramo dedicado pro 409 de concorrência otimista (`problem.detail`, mensagem de "recarregue e tente de novo"). **Segundo achado no mesmo arquivo**: `api.ts`/`serviceUtils.ts` também estavam com a extração de dado de sucesso quebrada pra TODOS os catálogos — um cast `as AxiosResponse<ApiResponse<T>>` puro de TypeScript, sem transformação em runtime, enquanto o backend real devolve DTO/`PagedResult<T>` cru desde a §3a; corrigido junto (mesmo arquivo já em edição, tratado como correção de correção em vez de decisão arquitetural nova): `getAll` desembrulha `PagedResult<T>.items`, os demais usam `res.data` cru. `api.ts` também perdeu o interceptor morto de `Authorization: Bearer` (lia um cookie `auth_token` que nunca existiu — a §7 usa sessão via cookie HttpOnly) e ganhou `withCredentials: true` (necessário pro cookie de sessão real ser enviado). `Product.ts`: **terceiro achado** — `highCost`/`expirationControlled` estavam tipados como o enum numérico `YesNo` (1/2), mas o backend manda `bool` de verdade; `product.expirationControlled === YesNo.YES` (`true === 1`) nunca era verdadeiro, quebrando silenciosamente o estado das duas checkboxes em todo carregamento — corrigido pra `boolean` puro, removendo o import de `YesNo` de `ProductForm.tsx`. `ProductForm.tsx` ganhou `rowVersion`/`isActive` no estado inicial (fluem automaticamente no PUT já que `data` inteiro é enviado). `ProductOverview.tsx`: mensagem de confirmação de exclusão corrigida de "removido permanentemente" pra refletir que agora é inativação reversível. Novo `ProductForm.test.tsx` — primeiro teste de componente/formulário do projeto (nenhum precedente de `render()`/`@testing-library/user-event` existia antes), 4 casos: submissão válida, erro de validação (Problem Details `errors`), conflito 409 por `RowVersion` obsoleto (confirma que o valor lido no GET é reenviado inalterado no PUT) e a correção do bug `YesNo`/bool (checkboxes refletindo o `bool` real do backend).
- [x] 9.8 Adicionar testes de integração do ciclo de produto, referências inválidas e limite sem movimentos/lotes. `ProductControllerTests.cs` (8 testes): ciclo completo criar→editar→inativar→reativar, `ProductTypeId`/`UnitOfMeasureId` inexistente ou inativo → 422 (3 testes), inativar um `ProductType`/`UnitOfMeasure` com produto ativo vinculado → 422 (prova a proteção da 9.3 do lado do dependente), busca por descrição/nome genérico, e um teste explícito de que `CurrentStock`/`StockValue` são graváveis direto via `Put` — a ausência de qualquer endpoint de movimento/lote já é a prova do "limite sem movimentos/lotes", não precisa de teste negativo elaborado. Três achados de bug corrigidos durante a escrita destes testes, documentados na 9.4/9.3 e abaixo: permissões faltantes, `[Range(0, double.MaxValue)]` num campo `decimal`, e um bug de xmin no `Remove`/`Activate`. **Achado de validação**: `ProductCreateRequest` usava `[Range(0, double.MaxValue)]` em `MinimumStock`/`CurrentStock`/`UnitPrice` (todos `decimal`) — anti-padrão conhecido do .NET: converter `double.MaxValue` pra `decimal` na comparação estoura, rejeitando QUALQUER valor válido com 400; corrigido pra `[Range(0, int.MaxValue)]` (converte limpo e é mais que suficiente pra estoque/preço). **Achado de concorrência (o mais sério dos três)**: a primeira versão de `ToggleActiveAsync` copiou o padrão de destacar-e-reanexar do `Update` (usado ali pra trocar de instância) — mas `Remove`/`Activate` recebem uma entidade que já está rastreada pelo `ChangeTracker` desde o `GetById`, com o xmin real já capturado; destacar essa mesma instância descarta a shadow property `"Version"` (não existe em memória fora do `ChangeTracker`), fazendo o `SaveChanges` comparar contra um xmin em branco e lançar um `DbUpdateConcurrencyException` falso — 409 em TODA inativação/reativação. Corrigido: só anexa se `entry.State == EntityState.Detached`; a instância já rastreada é mutada diretamente. Diagnosticado via `--logger "console;verbosity=detailed"` depois de descartar uma pista falsa (um teste de RowVersion obsoleto que também esperava 409, por motivo diferente).
- [x] 9.9 Executar testes de regressão em plano de saúde, cargo, religião, fornecedor, fabricante, transportadora, grupo, tipo e unidade. Confirmado antes de começar: nenhum dos 9 catálogos tinha teste de integração (`ProductGroupControllerTests.cs`/`ReligionControllerTests.cs` existiam mas não foram achados na varredura inicial de planejamento — `grep` procurou por `*Carrier*`/`*ProductType*`/`*Catalog*`, não pelos nomes reais dos arquivos). Rodar a suíte completa após as mudanças da 9.1–9.3 revelou 5 regressões reais: `ProductGroupControllerTests.Post_ValidProductGroup_ReturnsCreatedWithEntity`/`Put_WithStaleRowVersion_ReturnsConflict` usavam nomes fixos ("Medicamentos", "Suplementos") que colidiam com o seed agora unicamente restrito — corrigidos com sufixo `Guid.NewGuid()`; `Delete_ExistingId_ReturnsNoContent` (ProductGroup e Religion) falhava pelo bug de xmin da 9.8 (resolvido junto). Suíte final: **144/144 passando** (28 testes unitários + 116 de integração, incluindo os 8 novos de Produto e a cobertura básica de create/list/inativar/reativar que os 9 catálogos ganharam nesta seção, já que a mudança de comportamento de `Remove`/`Get` os afeta diretamente e nenhum deles tinha cobertura antes). Front-end: `npm run build`, `npm run lint` e `npm test` (15/15 — 11 de `serviceUtils.test.ts` + 4 novos de `ProductForm.test.tsx`) todos limpos em `SeniorStockManager-Frontend/SeniorStockManagerFrontend`.

## 10. Identidade, acesso e contratos nos front-ends

- [x] 10.1 Criar contexto tipado de autenticação e autorização compartilhável conceitualmente pelos dois front-ends. `AuthContext`/`AuthProvider` (`src/contexts/AuthContext.tsx`, mesmo padrão de `ThemeContext` já existente — Context+Provider simples, sem lib externa) implementado de forma idêntica (copy-paste, mesma convenção usada por `api.ts`/`serviceUtils.ts`/`Table`/`FormControls` nos dois projetos) em `SeniorCareManagerFrontend` e `SeniorStockManagerFrontend` — "compartilhável conceitualmente" não significa um pacote/workspace novo, decisão confirmada no plano dado que nenhuma outra parte do projeto usa código literalmente compartilhado entre os dois front-ends. `useAuth()` (`src/hooks/useAuth.ts`) encapsula `useContext` com checagem de `undefined` (melhora o precedente do `ThemeContext`, que usa um valor-padrão não-nulo). `CurrentIdentity`/`EffectivePermission` (`src/types/models/`) espelham `CurrentIdentityDTO`/`EffectivePermissionDTO` exatamente.
- [x] 10.2 Implementar login, ativação, recuperação, troca de senha, cadastro de MFA e uso de código de recuperação. `authService.ts` (`src/features/auth/services/`, bespoke — não passa por `generateGenericMethods`, que assume CRUD de uma entidade) com um método tipado por endpoint de `api/v1/Auth`. Telas novas: `LoginPage`/`LoginForm` (reescritos — eram maquete estática sem `onSubmit` nos dois front-ends), `MfaChallengePage` (desafio de login, com aviso quando restam ≤2 códigos de recuperação), `MfaEnrollPage` (cadastro voluntário ou obrigatório, mostra `otpAuthUri`/chave manual — sem lib de QR code no projeto, texto é a implementação honesta em vez de uma dependência nova só pra isso — e os códigos de recuperação uma única vez após confirmar), `RecoverAccountPage`, `ResetPasswordPage`, `ActivateAccountPage`, `ChangePasswordPage`. **Achado corrigido**: `AuthController.Login`/`LoginMfa` devolvem 401 com corpo real (`MessageResponse{Message:"Credenciais inválidas."}`), diferente do 401 vazio do middleware de cookie — `handleServiceError` (compartilhado por TODO service) tratava todo 401 com a mesma mensagem genérica "Não autorizado", escondendo a mensagem real do backend; corrigido pra só usar a mensagem genérica quando o corpo não tem `message` (mesmo ajuste cobre o 429 do limitador de origem, que usa o mesmo formato).
- [x] 10.3 Manter acesso somente em memória e renovação em cookie protegido, sem token em armazenamento persistente acessível a script. `AuthContext` guarda `identity`/`status` só em `useState` — nenhuma escrita em `localStorage`/`sessionStorage` em lugar nenhum do fluxo de auth. Renovação de sessão é toda server-side (`OnValidatePrincipal`, já existente desde a §7) — o front-end não gerencia token nenhum. **Achado**: `js-cookie`/`@types/js-cookie` estavam nos dois `package.json` mas não eram mais usados em nenhum dos dois front-ends desde a remoção do interceptor `Authorization: Bearer` morto (stock, §9; care, corrigido nesta seção) — removidos (`npm uninstall`) dos dois projetos.
- [x] 10.4 Implementar restauração e compartilhamento da sessão, redirecionamento sem loop após HTTP 401 e tratamento explícito de HTTP 403. Restauração: `AuthProvider` chama `authService.me()` (`GET /auth/me`) uma vez ao montar — se o navegador já tiver o cookie de sessão (de uma visita anterior, inclusive no OUTRO front-end, já que os dois observam o mesmo cookie por origem/domínio configurado desde a §7.4), a sessão volta sem novo login; "compartilhamento" é automático, sem mecanismo extra. 401: interceptor de resposta em `api.ts` (`registerUnauthorizedHandler`, callback registrado pelo `AuthProvider` via `useEffect` — não importa o router diretamente ali pra não criar dependência circular com as rotas de cada feature, mesmo motivo do `useAppRoutes.ts` existir) ignora `Auth/me`/`Auth/login` (esses 401 são resultado esperado do próprio fluxo de login/restauração, não sessão expirada) e, pra qualquer outro 401, limpa a identidade em memória e `window.location.assign('/login')` só se `pathname` já não for `/login` (guarda contra loop). 403: `RequireAuth` (rota) mostra "Acesso negado" inline — não desloga, não redireciona, distinção explícita do 401.
- [x] 10.5 Consumir o contexto atual para menus e ações, mantendo claro que a API é a autoridade final. `RouteDefinition` ganhou `requiredPermission?:{resource,action,feature?}` e `hideFromNav?`. `RequireAuth` (`src/features/auth/components/`, novo — guarda de rota por componente, o projeto não usa loaders do React Router) acha a rota atual por `matchPath` contra `routes` (mesma técnica de `useBreadcrumbs.ts`) e checa `hasPermission` contra `requiredPermission` antes de renderizar. `Header` ganha nome do usuário + botão "Sair"; `Sidebar` e a página `Registrations`/`Cadastros` (stock e care) filtram itens por `hasPermission` quando a rota associada declara `requiredPermission` — itens sem essa declaração continuam sempre visíveis. Todas as rotas de catálogo existentes (9 no stock, 3 no care) ganharam `requiredPermission` retroativamente pra esse mecanismo ter efeito real — a checagem de UI é só conveniência (evita links mortos/403 surpresa), a API continua sendo quem decide de fato.
- [x] 10.6 Criar telas administrativas de usuários, estados de conta e disparo de ativação/recuperação sem campo de senha administrativa. Só no front-end care (decisão do usuário — console único, stock não duplica). `AdminUserOverview` (lista + criar) + `AdminUserStateModal` (troca de estado combinando select + senha atual, reautenticação). "Disparo de recuperação" não precisou de endpoint novo — reaproveita o `POST /auth/recover` self-service já existente. Nenhum campo de senha administrativa em lugar nenhum (a conta nasce `PROVISIONED`, só ganha credencial pelo fluxo de ativação por token). **Achado corrigido após revisão do PR (`AdminUserController.Post` devolvia o token de ativação na resposta)**: a primeira versão desta tarefa devolvia o `ActivationToken` na resposta do `POST /AdminUser` e mostrava numa mensagem na tela — a revisão apontou corretamente que isso contradiz a spec `platform-authentication` ("Senhas, códigos MFA, tokens de ativação, recuperação e sessão SHALL NOT aparecer em logs, respostas administrativas ou exportações", sem ressalva pra resposta de criação) e o próprio `design.md` do change (risco "Canal de ativação indisponível em ILPI de baixo orçamento", mitigante já documentado: "definir um modo operacional de entrega do link de uso único sem imprimir ou registrar o token"). Revertido: `AdminUserController.Post` volta a devolver `AdminUserDTO` puro (o `ActivationToken` retorna de `AdminUserService.CreateAsync` mas é descartado no controller, igual estava antes) — coberto por `Post_DoesNotIncludeActivationTokenInResponse`. A tela mostra só uma mensagem neutra pedindo pra seguir "o procedimento institucional" pra entrega segura da ativação — **gap operacional reconhecido, não resolvido nesta seção**: sem serviço de e-mail, a entrega de fato do link/token pra pessoa nova continua sem um canal técnico definido; o único precedente no projeto é o bootstrap do primeiro admin (`Program.cs`), que imprime o token só no console do processo na instalação (acesso de servidor, não de tela web) — não generaliza pra criação de conta rotineira via UI. Documentado aqui como trabalho futuro, não inventado um mecanismo novo só pra fechar esta tarefa.
- [x] 10.7 Criar telas de papéis, grupos, permissões, responsabilidades, atribuições, exceções e políticas com escopo e validade visíveis. Só no front-end care. `RoleOverview`/`PermissionGroupOverview`/`OrganizationalRoleOverview` (CRUD com concorrência otimista via `RowVersion`, mesmo padrão de `ReligionOverview`/`ProductGroupOverview` das seções anteriores). **Achado que limitou o escopo, documentado no plano**: `AdminRoleController`/`AdminPermissionGroupController`/`AdminOrganizationalRoleController` só expõem `POST`/`DELETE` pra vincular/desvincular grupos de permissão e usuários — nenhum `GET` pra listar o que já está vinculado hoje. Construir uma UI de "gerenciar vínculos" sem conseguir mostrar o estado atual seria enganoso (o usuário não teria como saber o que já existe antes de adicionar/remover) — essas 3 telas ficaram CRUD do registro em si, sem gestão de vínculo, até o backend ganhar um endpoint de leitura. `OrganizationalRoleAssignmentOverview` e `UserPermissionOverrideOverview`, ao contrário, TÊM `GET` real (`?userId={guid?}`) e escopo/validade são campos do próprio DTO — essas duas ganharam listagem completa (nome do usuário/papel resolvido client-side a partir de `AdminUser`/`OrganizationalRole`) + criar + encerrar antecipadamente (`PUT {id}/end` ou `/revoke`), com tipo de escopo (Instituição/Unidade/Setor) e datas de validade visíveis na tabela, exatamente como pedido. `AccessPolicyOverview` cobre a versão/estado (rascunho/ativa/retirada) com ações de revisar (nova versão, `POST {id}/revise`), ativar e retirar. Novo `permissionService.ts` (`GET /AdminPermission`, vocabulário fixo de permissões) alimenta os seletores de Resource/Action/Feature em `UserPermissionOverride`/`AccessPolicy` — evita campo de texto livre sujeito a erro de digitação que quebraria silenciosamente a permissão criada.
- [x] 10.8 Criar telas de parâmetros de segurança e sessões ativas com confirmação e reautenticação nas ações críticas. Só no front-end care. `InstitutionSecurityPage` (registro único, não lista — `GET`/`PUT` de `MaxFailedAttempts`/`LockoutDurationMinutes`/`AccessTokenDurationMinutes`/`RefreshTokenDurationDays`/`MfaRequiredForAllUsers`) e `UserSessionOverview` (sessões de um usuário, aninhada sob `AdminUser` via `/admin-user/:userId/sessions`, com revogar uma ou revogar todas). Ambas usam o novo `ReauthModal` (`src/components/Modal/`, compartilhado) — senha atual de quem está agindo, não da conta afetada, mesmo padrão que `AdminUserController.ChangeState`/`AdminUserSessionController.Revoke` já exigem no backend desde a §6/§7. **Limitação aceita, documentada no plano**: `UserSessionDTO` não tem uma flag "esta é minha sessão atual" e `/auth/me` não devolve `session_id` (cookie é `HttpOnly`, ilegível por JS) — a lista de sessões não destaca a sessão em uso; corrigir isso exigiria uma mudança de backend fora do escopo desta seção.
- [x] 10.9 Substituir o `ApiResponse<T>` divergente por clientes tipados compatíveis com recursos, paginação e Problem Details. **Achado maior**: o front-end de estoque já tinha sido corrigido na §9 (cast falso `AxiosResponse<ApiResponse<T>>`, sem `withCredentials`, interceptor morto de `Authorization: Bearer`/`js-cookie`) — mas o front-end care (`SeniorCareManagerFrontend`) nunca recebeu a mesma correção e estava rodando exatamente o padrão quebrado: `getAll()` lendo `res.data.data` de um corpo que na verdade é `{items,page,pageSize,totalCount}`, `getById`/`create`/`update` lendo `res.data.success`/`res.data.data` de DTOs crus. Isso quebrava silenciosamente as 3 telas de catálogo do care (`Religion`/`Position`/`HealthInsurancePlan`) em produção. Corrigido portando literalmente a mudança da §9 (`api.ts` sem envelope falso + `withCredentials:true` + interceptor 401/403 novo; `serviceUtils.ts` desembrulha `PagedResult<T>.items`, `handleServiceError` parseia Problem Details real) — os 8 domínios administrativos novos (§10.6-10.8) e as 3 telas de catálogo existentes do care passam a usar o cliente corrigido. `TextInput` (`src/components/FormControls/`, compartilhado pelos dois front-ends) ganhou os tipos `'password'`/`'date'` (só suportava `'text'|'email'|'number'`) — necessário pra login/MFA/recuperação e pros campos de validade das novas telas administrativas.
- [x] 10.10 Padronizar estados de carregamento, vazio, validação, conflito, indisponibilidade e repetição segura nos fluxos. Carregamento: `RequireAuth` mostra um estado de espera enquanto a sessão restaura (`status==='loading'`), evitando um flash de redirecionamento pro login. Validação/conflito: `handleServiceError` (10.9) já cobre 400 (dicionário de erros de campo), 409 (concorrência otimista, mensagem de "releia e tente de novo") e 422 (regra de negócio) de forma uniforme — reaproveitado por toda tela nova sem lógica duplicada. Indisponibilidade/erro genérico: `AlertModal` (já existente) reutilizado em todas as telas administrativas novas, mesmo padrão de sucesso/erro das telas de catálogo. **Achado corrigido, fora do escopo nominal desta tarefa mas bloqueando os próprios testes desta seção**: `FormModal` (`src/components/Modal/`, compartilhado pelas duas apps) não capturava a exceção que `useModalForm.handleSubmit` relança de propósito quando `onSubmit` falha (pra manter a modal aberta) — todo fluxo de criar/editar com erro gerava uma promise rejeitada sem handler (reproduzível em QUALQUER tela que já usava `FormModal`, incluindo `Religion`/`Position`/`HealthInsurancePlan`/`ProductGroup`/etc. das seções anteriores, não só as novas). Corrigido com um `try/catch` em `FormModal.handleFormSubmit` — a modal continua abrindo/fechando exatamente como antes, só para de vazar a rejeição pro console/test runner.
- [x] 10.11 Adicionar testes de login, MFA, logout, expiração, rota protegida, visibilidade por permissão, administração de acesso e falhas de CRUD. Componente/service, mesmo padrão de `ProductForm.test.tsx` (§9) — mock de `@/features/api`, `render()`+`@testing-library/user-event` — não E2E de navegador real (`Cookie.SecurePolicy=Always` é incondicional e nenhum compose local tem TLS, só a camada `ops` opcional de produção; login não é testável em navegador real contra o stack local hoje, gap de infraestrutura pré-existente e fora do escopo desta seção). Replicado nos dois front-ends: login (sucesso "ok", credencial inválida com mensagem real do backend, `mfa_required`), `AuthContext` (restauração de sessão, `hasPermission`, redirecionamento 401 sem loop), `RequireAuth` (anônimo redireciona, autenticado com permissão passa, autenticado sem permissão mostra "Acesso negado" inline), `Header` (usuário/logout), `MfaChallengePage` (sem token redireciona, confirmação válida/inválida) — 34 testes no care (9 arquivos) e 32 no stock (7 arquivos). Care, adicional: `RoleOverview` (criar, 409 de concorrência, excluir), `PermissionGroupOverview` (422 de validação), `UserPermissionOverrideOverview` (criação completa com seletor de usuário/permissão). `Table` (`src/components/Table/`, compartilhado) precisou ser generalizado de `T extends {id:number}` pra `T extends {id:number|string}` — todos os 8 domínios administrativos novos usam Guid como id, e o componente antes só aceitava id numérico (catálogos §3b/§9); mudança compatível, screens com id numérico continuam funcionando sem alteração.

## 11. Baseline de acessibilidade

- [x] 11.1 Corrigir semântica, nome acessível e foco visível em botões, campos, busca, tabela, cabeçalho e navegação dos dois front-ends. `FormField` (`src/components/FormControls/FormField/`) ganhou `htmlFor`/`id` reais (antes o `<label>` era irmão solto do input, sem associação nenhuma) e um modo `groupLabel?:boolean` que troca `<label>` por `<fieldset>/<legend>` — um grupo de checkboxes não tem "o" controle único que um `htmlFor` possa apontar. `TextInput`/`SelectInput`/`Checkbox`/`CheckboxGroup` ganharam `useId()` (id estável sem prop-drilling manual) + `aria-invalid`/`aria-describedby` (ver 11.3) + indicador de foco visível de verdade (`focus:outline-none focus:border-neutralDarker` → `focus:outline-none focus:ring-2 focus:ring-primary`, mesmo padrão que `Card` já usava). `AccessibilityBar` (3 botões só-ícone) e o botão de recolher/expandir + itens de navegação recolhidos do `Sidebar` ganharam `aria-label` (dinâmico no de alto contraste, via `aria-pressed`/`aria-expanded`). `SearchBar`: estoque não tinha `onSubmit` no `<form>` — Enter no campo de busca não fazia nada, bug real de operabilidade, não só cosmético; os dois apps ganharam `aria-label='Buscar'` no botão só-ícone. `Card` (cards de navegação do `Registrations`/`AdminOverview`) virou de fato operável por teclado (`role='button'` + `onKeyDown` ativando em Enter/Espaço + `aria-label`), antes só tinha `tabIndex={0}` sem nenhuma forma de ativar. Sweep mecânico de `aria-label`/`title` em botões de ação só-ícone (editar/excluir) em TODAS as telas `*Overview.tsx` dos dois apps que ainda não tinham (a maioria — só as telas administrativas da §10 já tinham `title`; achado extra durante o sweep: `PermissionGroupOverview` também estava sem, apesar de ter sido construída na §10 junto com `RoleOverview`, que tinha). Estoque, só: removido o `<h1>` duplicado (`BreadcrumbPageTitle` + um próprio) em `ProductForm`/`CarrierForm`/`SupplierForm`, rebaixado pra `<h2>` (preserva o texto da seção, que em `CarrierForm` difere do título da página — não é puramente redundante). **Achados adicionais, fora da lista original, pegos pelo `jest-axe` (11.6) e pela passagem manual de teclado (11.7)**: botão de mostrar/ocultar senha em `LoginForm` (os dois apps) sem `aria-label` nenhum; "Esqueceu sua senha?" era um `<p onClick>` sem `tabIndex`/`role`/`onKeyDown` — completamente inalcançável só por teclado, na própria jornada de login; coluna de checkbox do `Table` compartilhado tinha `<th>` vazio (falha `empty-table-header`) e o `<input type="checkbox">` de cada linha não tinha rótulo nenhum. Todos corrigidos (aria-label no botão de senha; "Esqueceu sua senha?" virou `<button type='button'>`; `<th>` ganhou `<span className='sr-only'>Selecionar</span>`; checkbox de linha ganhou `aria-label` com o número da linha). Contraste: computado à mão pela fórmula de luminância relativa do WCAG a partir dos valores reais de `index.css` — `--color-primary` do care (`#009f96`) contra branco dava ~3.28:1 (abaixo do mínimo 4.5:1 pra texto normal), escurecido pra `#008078` (~4.82:1, mesmo matiz verde-água); `--color-text-secondary` (`#6d8385`, compartilhado pelos dois apps) contra branco dava ~4.01:1, escurecido pra `#647879` (~4.66:1). Estoque, `--color-primary` (`#005ca4`) já passava (~6.85:1) e não foi tocado — ajuste mínimo dos dois tokens que falhavam, não um redesign.
- [x] 11.2 Implementar gerenciamento de foco, fechamento por teclado e retorno ao acionador em todos os modais. `BaseModal.ModalRoot` (`src/components/Modal/`, compartilhado) ganhou: captura do elemento focado antes de abrir, foco automático no primeiro elemento focável do modal (ou no container, se não houver nenhum) ao abrir, um `keydown` em `window` que prende Tab/Shift+Tab dentro do modal enquanto aberto (Escape já fechava desde antes desta seção), e devolução do foco ao elemento capturado quando fecha — implementação própria (~20 linhas), sem dependência nova. `ModalHeader`'s botão de fechar (X) ganhou `aria-label='Fechar'` (antes só-ícone, zero nome acessível). **Achado crítico, pego pelos testes de componente ANTES de qualquer commit** (ver seção Erros do processo): a primeira implementação prendia o efeito de foco em `[isOpen, onClose]` — como todo `onClose` passado a um modal (ex.: `RoleFormModal`'s `handleClose`) é recriado a cada render sem `useCallback`, e digitar num campo do formulário dentro do modal recria o componente-formulário (logo `onClose`), o efeito reexecutava a CADA TECLA digitada, roubando o foco de volta pro primeiro elemento focável no meio da digitação — sintoma observado: `postMock` chamado com `{name:"E"}` em vez do texto completo digitado. Corrigido com um `onCloseRef` (`useRef(onClose)` atualizado a cada render por um efeito separado sem deps) — o efeito de foco ficou só com `[isOpen]` na dependência, chamando `onCloseRef.current()` no lugar de `onClose` direto. Verificado com as suítes completas dos dois apps voltando a 100% verde. **Limitação conhecida, não corrigida nesta seção** (achada pela passagem manual de teclado, 11.7): o retorno de foco depende de que o controle que abriu o modal continue sendo o MESMO nó do DOM depois do modal fechar; os botões de ação Editar/Excluir de toda tela `*Overview` são renderizados por um sub-componente `Actions` declarado dentro do corpo do próprio componente pai (identidade de função nova a cada render — padrão repetido em ~20 telas nos dois apps), então o React desmonta/remonta esses botões a cada re-render do Overview, inclusive o causado por abrir o próprio modal; o nó que estava focado deixa de existir antes do efeito de foco capturar a referência, e o foco acaba em `document.body` em vez de voltar pro botão (continua alcançável via Tab a partir do topo, só perde a posição). Não afeta modais abertos a partir de um botão estável fora do `Actions` (ex.: "Adicionar" — verificado funcionando corretamente). Saneie o padrão `Actions` (memoização/estabilidade de referência) numa seção futura de manutenção; um sweep de ~20 arquivos nos dois apps foge do escopo mecânico desta seção. Também não pursuido: `aria-labelledby` ligando o `<h3>{title}</h3>` do `ModalHeader` ao `role="dialog"` do container — `ModalRoot` não recebe `title` (é um `ModalHeader` irmão, composto por cada modal consumidor) e nem todo modal define título; troca de escopo/API do `Modal` julgada desproporcional ao ganho, documentada aqui como limitação reconhecida em vez de ignorada silenciosamente.
- [x] 11.3 Associar mensagens de validação aos campos e anunciar erros e resultados sem depender apenas de cor. `FormField` renderiza o texto do erro num `<span id={`${id}-error`}>`; `TextInput`/`SelectInput`/`Checkbox`/`CheckboxGroup` aplicam `aria-invalid={!!error}` e `aria-describedby={error ? \`${id}-error\` : undefined}` no elemento nativo — o encanamento fica correto e pronto de ponta a ponta. **Nota de escopo**: a prop `error` já existia antes desta seção mas nenhuma tela do projeto a usa hoje (toda validação passa por `AlertModal`, mensagem solta sem associação a campo) — esta tarefa conserta o mecanismo existente, não inventa validação de campo nova onde não havia; o primeiro uso real de `error` (sempre que alguma tela passar a validar campo a campo) já nasce acessível sem precisar de outra mudança.
- [x] 11.4 Validar e normalizar preferências persistidas de contraste e fonte, incluindo restauração do padrão. `ThemeContext` (`src/contexts/`, compartilhado): a leitura inicial de `localStorage` no `useEffect` de montagem antes aplicava `fontSize` sem limite nenhum (só `changeFontSize` em runtime limitava a [10,44]) — um valor corrompido no `localStorage` passava direto, violando o cenário "Valor persistido inválido" da spec; corrigido com a mesma função `clampFontSize` usada nos dois pontos. `theme` também passou a ser validado contra `VALID_THEMES=['light','high-contrast']` na leitura inicial, caindo pro padrão `'light'` se o valor salvo for desconhecido. **Decisão registrada**: `changeFontSize` mudou de REJEITAR silenciosamente valor fora do intervalo pra fazer CLAMP — produz o mesmo comportamento final pros botões +/-2 já existentes (não dá pra pular um limite em incrementos de 2 partindo de dentro do intervalo válido) e beneficia a nova `AccessibilityPage` (11.5), que expõe os mesmos controles.
- [x] 11.5 Substituir o placeholder da página de acessibilidade por instruções e controles reais. `AccessibilityPage.tsx` (os dois apps, era `<div>AccessibilityPage</div>`) ganhou conteúdo real: explicação de navegação por teclado (Tab/Shift+Tab, Enter/Espaço, Esc), e os MESMOS controles funcionais do `AccessibilityBar` (alternar alto contraste, aumentar/diminuir fonte) reaproveitando `ThemeContext` — sem duplicar lógica — pra quem chega direto nessa página sem passar pelo cabeçalho.
- [x] 11.6 Adicionar verificações automatizadas de acessibilidade para login, MFA, modal, tabela e formulário representativo. `jest-axe` (`^11.0.0`, + `@types/jest-axe` pro `tsc -b` não quebrar — o pacote em si não publica tipos) adicionado como devDependency nos dois apps; `expect.extend(toHaveNoViolations)` em `src/test/setup.ts`, `axe` pré-configurado em `src/test/axe.ts` com a regra `color-contrast` desligada (jsdom não renderiza estilo computado de verdade — limitação documentada do axe-core/jsdom; contraste é o cálculo manual da 11.1). Novo `src/test/accessibility.test.tsx` por app, 4 casos cada cobrindo os 4 padrões de UI do `design.md` (Button, FormControls, Table, Modal): login (`LoginForm`), um formulário avulso (`MfaChallengePage` no care, `CarrierForm` no estoque), uma listagem em `Table` (`RoleOverview`/`ManufacturerOverview`) e um modal de formulário aberto (`RoleFormModal`/`ManufacturerFormModal`). **Achados reais pegos na primeira rodada, antes de qualquer correção** (ver 11.1): botão de senha sem `aria-label` no login, `<th>` vazio e checkbox de linha sem rótulo no `Table` compartilhado — todos corrigidos e as suítes voltaram a passar 100% (care 4/4, estoque 4/4) só depois da correção, não ajustando o teste pra tolerar o achado.
- [x] 11.7 Executar e registrar teste manual somente por teclado no login e em um CRUD de cada front-end. **Ambiente**: backend/banco de dados não estavam disponíveis nesta sessão de trabalho (subir a stack completa — migrações, seed, segredos — seria um desvio grande e arriscado, fora do padrão desta seção); em vez de uma sessão manual ao vivo em navegador, a verificação foi mecanizada em `src/test/keyboardNavigation.test.tsx` (novo, por app) usando `userEvent.tab()`/`userEvent.keyboard()`, que dirige foco e eventos de teclado reais do DOM (jsdom) — evidência mais forte e reexecutável (protegida por CI) que uma nota manual pontual, cobrindo a mesma jornada exigida pelo cenário "Verificação manual de teclado" da spec: login (ordem de Tab completa, ativação por Espaço do botão de mostrar/ocultar senha) e o CRUD de referência de cada app (Religião no care, Transportadora no estoque — ordem de Tab, trap de foco dentro do modal incluindo o wrap Tab/Shift+Tab, Esc fechando e devolvendo o foco, ativação de Editar/Excluir por Enter). **Achados reais, descobertos pela própria passagem e corrigidos** (detalhados na 11.1): botão de mostrar/ocultar senha sem nome acessível; "Esqueceu sua senha?" completamente inalcançável por teclado (era um `<p onClick>`). **Achado descoberto e documentado, não corrigido** (detalhado na 11.2): retorno de foco pro botão de ação de linha (Editar/Excluir) que abriu um modal perde a posição (vai pra `document.body`) por causa do padrão `Actions` remontando a cada render — escopo de correção (~20 telas) maior que o desta seção.

## 12. CI, migração e entrega

- [x] 12.1 Adicionar `dotnet test` com resultados e cobertura ao job de backend. `ci.yml`, job `backend`: o comentário antigo ("sem projeto de teste ainda") estava desatualizado desde a §1 — o job só fazia `dotnet build` do `WebAPI` isolado e NUNCA buildava nem rodava `SeniorCareManager.UnitTests`/`IntegrationTests` (145 testes existentes nunca tinham rodado em CI). Trocado por `dotnet test SeniorCareManager.WebAPI.sln --configuration Release --logger trx --collect:"XPlat Code Coverage" --results-directory ./TestResults`, builda a solution inteira e roda os 145 testes (28 unitários + 117 integração, agora 118 com a §12.5). `coverlet.collector` já era dependência dos dois `.csproj` de teste — nenhum pacote novo. `TestResults/` publicado como artefato do job (`actions/upload-artifact@v4`) e verificado pelo `check-frontend-bundle.sh` estendido (12.4) contra credencial embutida. Comando testado localmente antes de ir pro workflow.
- [x] 12.2 Adicionar testes e cobertura aos jobs dos dois front-ends, preservando os filtros por caminho. `ci.yml`, jobs `care-web`/`stock-web`: `npm run test:coverage` (`vitest run --coverage`, provider `v8`, reporters `text`+`lcov` já configurados em `vite.config.ts` desde a §9/§10) adicionado logo após `npm audit`, antes do `npm run build` — só mais um `run:` dentro do job já existente, os filtros de path (`needs: changes`, `if: needs.changes.outputs.*_web == 'true'`) continuam intocados. `coverage/` publicado como artefato de cada job.
- [x] 12.3 Fazer o check agregado falhar quando qualquer build, teste, migração, lint ou gate de segurança aplicável falhar. Nenhuma mudança de infraestrutura de CI foi necessária: branch protection de `dev`/`main` já exige, como status checks obrigatórios (união — todos precisam passar), `ci-required`, `gitleaks-pr`, `codeql (csharp)`, `codeql (javascript-typescript)` e `security-sca-required` (confirmado via `gh api .../branches/dev/protection`). Como o `dotnet test`/`npm test` novos (12.1/12.2) entram DENTRO dos jobs `backend`/`care-web`/`stock-web` já cobertos por `ci-required` (que já falha se qualquer um não for `success`/`skipped` — `ci.yml` linhas do job `ci-required`), a falha já propaga automaticamente pro check agregado sem tocar branch protection (ação de escopo maior, deliberadamente evitada).
- [x] 12.4 Adicionar verificação automatizada de fixtures sintéticas e ausência de credenciais nos artefatos. Duas partes: **credencial em artefato** — `check-frontend-bundle.sh` (compartilhado com o bundle de front-end) generalizado pra também escanear `.trx`/`.xml` (antes só `.js`/`.css`), reaproveitado no job `backend` contra `TestResults/`; a versão original lia arquivo por linha em bash (`while IFS= read -r line`), o que com XML de cobertura de ~53 mil linhas por arquivo estourava timeout (centenas de milhares de fork/exec de grep) — reescrito pra rodar `grep` uma vez por ARQUIVO inteiro em vez de por linha, ~1.2s pro `TestResults/` real dos 145 testes. **Fixture sintética** (gap real, nada existia antes): novo `.github/scripts/check-synthetic-fixtures.sh`, escaneando só `SeniorCareManager.IntegrationTests`/`UnitTests` (backend) e `src/test`+`*.test.ts(x)` (os dois front-ends) — não o repo inteiro, pra não gerar falso positivo em exemplo de documentação. Extrai todo endereço de e-mail e falha se o domínio não estiver numa allowlist reservada (RFC 2606: `example.com/.org/.net`, `*.example`, `*.test`, `*.invalid`; RFC 6762: `*.local`; convenção do projeto: `exemplo.com.br`). Testado com um e-mail real (`@gmail.com`) injetado propositalmente — falha corretamente; removido, volta a passar. Chamado como novo passo no job `env-hygiene` (`ci.yml`), que já roda sempre.
- [x] 12.5 Testar migrações em banco vazio e em snapshot sintético da versão imediatamente anterior. Banco vazio já era coberto por toda a suíte via `PostgresWebApplicationFactory` (migra do zero antes de cada teste, desde a §1). Gap real: nenhum teste aplicava migração sobre dado PRÉ-EXISTENTE. Novo `MigrationUpgradeTests.cs`: sobe um Postgres efêmero próprio (Testcontainers), migra até `AddAuditEvent` (estado imediatamente anterior a `AddCatalogActiveState` — a migração mais recente com impacto real em tabela já populada: adiciona `is_active boolean NOT NULL DEFAULT true` a 6 tabelas de catálogo E dropa/recria FK+índice do `producttype`, não um caso trivial de "só cria tabela nova"), insere linha sintética em cada uma das 6 tabelas via SQL cru (não via `DbContext`/`DbSet`, que sempre reflete o modelo ATUAL — inserir pela ORM não simularia de verdade o estado histórico da tabela antes da migração), aplica `AddCatalogActiveState`, e confirma: nenhuma exceção, os dados sobrevivem com os mesmos valores, a coluna nova foi populada com o default `true`, e o vínculo FK `producttype→productgroup` sobrevive à recriação do índice/FK pela própria migração (caso mais delicado do teste). Mecanismo genérico (nome da migração como parâmetro) — reaproveitável pra outro par no futuro. Exatamente o cenário "Atualização de versão" da spec ("os dados de catálogo permanecem íntegros e as novas restrições são satisfeitas").
- [x] 12.6 Criar script de pré-validação dos dados e garantir falha antes de constraints incompatíveis. Gap real (nada existia antes) — `design.md` já registrava o risco ("Migração falha por dados atuais inválidos" → "executar pré-validação, produzir relatório e abortar antes de constraints destrutivas") sem mitigante implementado. Dividido em geração (CI) + execução (`deploy.sh`), porque o servidor de produção NUNCA compila (modelo build-once/deploy-many — não tem SDK .NET nem `dotnet-ef`, só Docker): `release.yml` ganhou um novo job `migration-script` (paralelo ao `build`), que instala o SDK e roda `dotnet ef migrations script --idempotent --output migration.sql` — o script já sai pronto envolto em `START TRANSACTION;...COMMIT;` (formato idempotente do EF Core, cada bloco de migração guardado por `IF NOT EXISTS(SELECT 1 FROM "__EFMigrationsHistory"...)`, então rodar contra um banco já parcialmente migrado só executa o que falta). Publicado como `releases/<versão>-migration.sql`, commitado no mesmo passo que já grava `releases/<versão>.env`. Novo `SeniorCareManager.WebAPI/dotnet-tools.json` (manifesto local, `dotnet tool install dotnet-ef --version 8.0.13`) fixa a versão da ferramenta de forma reprodutível em vez de instalar "latest" a cada run. `deploy.sh` ganhou `pre_validate_migration()`, chamada em `do_deploy()` logo depois de `pg_backup` e antes de `compose pull`: se o Postgres já está rodando (mesma guarda de `pg_backup` — sem Postgres rodando é o 1º deploy, nada pra validar) e existe o `migration.sql` do release, troca a última linha (`COMMIT;`) por `ROLLBACK;` via `sed` e roda contra o Postgres de PRODUÇÃO real via `docker exec -i <pg_cid> psql -v ON_ERROR_STOP=1` — detecta qualquer violação de constraint ANTES do deploy real (que só migra de fato quando o container novo da API sobe, via `Program.cs`), com ZERO risco de persistir qualquer coisa (rollback incondicional, independente do script ter tido sucesso ou falhado no meio) e sem precisar de SDK .NET no servidor (só o `psql` que a imagem `postgres:16-alpine` já traz). **Verificado manualmente de ponta a ponta nesta sessão**: subiu um Postgres isolado, migrou até `AddAuditEvent`, inseriu dado sintético, gerou o script idempotente real via `dotnet ef`, rodou o dry-run (sucesso, `exit=0`), confirmou por `\d religion` que a coluna `is_active` NÃO existe após o dry-run (rollback genuíno, zero persistência) e que os 4 registros de `religion` continuam intactos; testou também o caminho de falha injetando um erro SQL proposital — `ON_ERROR_STOP=1` aborta corretamente (`exit=3`).
- [x] 12.7 Atualizar Dockerfiles, Compose, nginx, release e health checks para a nova configuração, instituição e autenticação. nginx e health checks já estavam corretos (proxy `/api`, `HEALTHCHECK` em `/health/ready`, cobertos por `HealthEndpointTests` desde antes desta seção) — nenhuma mudança necessária. **Achado real**: `docker-compose.yml` (`infra/deploy/` e `infra/docker-test/`) só repassava 3 variáveis pro serviço `seniorcare-api` (`ASPNETCORE_ENVIRONMENT`, `ConnectionStrings__DefaultConnection`, `CORS_ALLOWED_ORIGINS`) — `Bootstrap__InstitutionName`/`Bootstrap__AdminEmail`/`Bootstrap__AdminDisplayName` (documentadas em `CONFIGURATION.md` desde a §7, implementadas no `BootstrapService`) NUNCA chegavam ao container mesmo definidas no `.env` do cliente, porque Compose só repassa o que está listado em `environment:`. Todo deploy até agora subia saudável mas NUNCA bootstrapava instituição/admin — falha silenciosa, já que as 3 vars ausentes = "nenhuma definida" é uma combinação válida pra `Program.cs`. Corrigido nos dois `docker-compose.yml` (com `${VAR:-}`, vazio por padrão — a validação "todas juntas ou nenhuma" já existe em `Program.cs`) e documentado nos dois `.env.example` (`infra/deploy/clients/exemplo/`, `infra/docker-test/`). **Confirmado corrigido de verdade pelo smoke test da 12.8** — só depois desta mudança o bootstrap passou a acontecer.
- [x] 12.8 Executar smoke test coordenado de prontidão, bootstrap, ativação, login, MFA, permissões e produto no ambiente Docker de teste. Executado de verdade nesta sessão (Docker disponível, diferente do bloqueio de backend/DB da §11): subiu `infra/docker-test/docker-compose.yml` completo (Postgres+API+os dois front-ends, build real das 3 imagens) com as 3 variáveis de Bootstrap definidas — sequência completa via `curl`: `/health/ready` → `200 Healthy`; log do container mostrou `Bootstrap: instituição e administrador PROVISIONED criados` + token de ativação (confirma a correção da 12.7 de verdade — é a primeira vez que esse caminho de deploy bootstrapa algo); `POST /Auth/activate` com o token → `200`; `POST /Auth/login` → `mfa_enrollment_required`; `POST /Auth/mfa/enroll` → chave TOTP; TOTP calculado localmente (HMAC-SHA1 puro, sem lib nova) a partir da chave; `POST /Auth/mfa/confirm` com o código real → sessão completa, `effectivePermissions` do papel "Administrador" retornado (dezenas de permissões, confirma toda a cadeia Papel→Grupo→Permissão resolvendo em produção real); acesso anônimo a `GET /api/v1/Product` → `401` (bloqueio confirmado); CRUD de Produto fim-a-fim autenticado — criar (com `UnitOfMeasure`/`ProductGroup`/`ProductType` de apoio), ler, atualizar (com `rowVersion`, concorrência otimista mudando de 798→804), excluir (`204`) — todos com sucesso; confirmado que os dois front-ends respondem (`200`) e que o proxy nginx `/api` do `care-web` encaminha corretamente pro backend (`401` sem cookie, mesma resposta que direto na API). Stack derrubada ao final (`docker compose down -v`), nada deixado rodando.
- [x] 12.9 Documentar implantação, bootstrap, canal de ativação, backup pré-deploy, rollback e incompatibilidade com clientes antigos. Novo `infra/deploy/BOOTSTRAP.md`, linkado de `infra/deploy/README.md` e do `README.md` raiz — runbook verificado contra a sequência real executada na 12.8 (não especulativo): variáveis antes do 1º deploy, captura do token do log (`docker logs ... | grep -A1 "Token de ativação"`), ativação via API/UI, aviso de MFA obrigatório. **Canal de ativação** (gap já reconhecido em 10.6, não resolvido nesta seção): documentada a orientação operacional pra contas administrativas criadas DEPOIS da primeira (que não têm o atalho do log de boot) — entrega por canal institucional já confiável fora da plataforma, confirmação de identidade pelo procedimento já em uso, sem inventar mecanismo técnico novo. **Backup/rollback**: só aponta pra `pg_backup()`/`do_rollback()`/`pre_validate_migration()` já implementados em `deploy.sh` (12.6), não duplica a lógica. **Incompatibilidade entre versões**: aviso explícito pra nunca trocar a imagem de só um dos três serviços manualmente — o release manifest já garante deploy atômico dos três juntos.
- [x] 12.10 Atualizar README, documentação de API e relatório de avaliação com as evidências finais de aderência. `README.md` raiz ganhou link pro novo `BOOTSTRAP.md`. `SeniorCareManager-Backend/README.md` — era uma única frase em inglês desatualizada ("Backend SeniorCare Manager developed in C# with PostgreSQL") — reescrito com stack, estrutura dos 3 projetos, como rodar/testar/migrar, e links pra `CONFIGURATION.md`/`BOOTSTRAP.md`/CI-CD. `docs/relatorio-avaliacao-requisitos-implementacao.md` (395 linhas, datado de 6/ago — ANTES de toda a `stabilize-existing-platform`, commit `d3b68cd`) ganhou uma seção final "14. Atualização pós stabilize-existing-platform" (datada, sem reescrever o relatório original) mapeando diretamente os gaps que a própria seção 12/13 do relatório apontava (backend não compilava em CI, sem auditoria WCAG, sem autenticação/autorização/auditoria real) contra o que esta mudança de fato entregou — deixando explícito que o parecer de "não pronto para dado real de saúde" continua válido (núcleo assistencial não foi tocado), só a fundação técnica que a seção 12 pedia como pré-requisito agora existe.

## 13. Aceite da mudança

- [ ] 13.1 Executar todos os cenários das cinco specs e registrar evidências automatizadas ou manuais conforme definido.
- [ ] 13.2 Confirmar que os três componentes compilam em checkout limpo e que o Graphify está atualizado.
- [ ] 13.3 Confirmar que nenhum endpoint administrativo aceita acesso anônimo e que toda escrita exige permissão efetiva.
- [ ] 13.4 Confirmar isolamento institucional, negação padrão, precedência, MFA administrativo e revogação de sessão por testes dedicados.
- [ ] 13.5 Confirmar que profissão não concede acesso e que nenhum dado clínico, prontuário, dashboard ou assinatura foi introduzido por esta mudança.
- [ ] 13.6 Validar a mudança OpenSpec em modo estrito e preparar o handoff para revisão e implementação.
